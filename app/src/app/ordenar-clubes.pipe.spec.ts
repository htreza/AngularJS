import { OrdenarClubesPipe } from './ordenar-clubes.pipe';

describe('OrdenarClubesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarClubesPipe();
    expect(pipe).toBeTruthy();
  });
});
