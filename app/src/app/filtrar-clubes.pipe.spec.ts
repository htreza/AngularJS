import { FiltrarClubesPipe } from './filtrar-clubes.pipe';

describe('FiltrarClubesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarClubesPipe();
    expect(pipe).toBeTruthy();
  });
});
